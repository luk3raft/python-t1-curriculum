ages = [23, 16, 5, 41, 14]

length = len(ages) # Getting the length of the ages list
print("length of ages list:", length)

last = ages[length-1] # Accessing the last element using length
print("last age using length:", last)

first = ages[0] # Accessing the first element
print("first age:", first)